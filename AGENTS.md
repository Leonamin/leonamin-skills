# leonamin-skills Agent Rules

## Lazy Loading

- For Git work, load the `git-workflow` skill before running Git commands.
- Keep each skill as `<skill-name>/SKILL.md` at this repository root.
- After editing a skill or `install.sh`, run:

```bash
bash -n install.sh
./install.sh all
```
